import entities.entitiy.Animal;
import org.junit.jupiter.api.Test;
import simulation.Simulation;
import simulation.ThreadAnimalLife;
import start.Config;

class SimulationTest {

    @Test
    void stepSimulation() throws Exception {
//
//Config config=new Config();
//config.setConfigurations();
//Simulation simulation=new Simulation();
//simulation.threadPlantGrow();
//simulation.stepSimulation();
//      for (Animal animal:simulation.getPlayingField().cellSet[0][0].zoo) {
//            simulation.playingField.cellSet[0][0].replicate(animal);
//                    }
//        Animal animal = simulation.getPlayingField().cellSet[0][0].zoo.get(1);
//
//
////        simulation.playingField.moveAround(animal,0,0);



//        Thread thread = new ThreadAnimalLife(animal, 0, 0);
//        thread.start();
//       int r= simulation.getPlayingField().cellSet[0][0].zoo.size();
// simulation.playingField.cellSet[0][0].plantGrow();
//      simulation.playingField.moveAround(new Buffalo(1,1),1,1);
////
//      simulation.treadLifeSimulation();
//      simulation.playingField.report();
//        var s=simulation.getPlayingField().cellSet[0][0];
//        simulation.playingField.moveAround();
//        var d=simulation.getPlayingField().cellSet[0][0];
//        Config config=new Config();
//config.setConfigurations();
//
//Simulation.stepSimulation();
//Simulation.stepSimulation();

    }
}